<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>مُعينك | Mueenk</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    
    <script>
        if (sessionStorage.getItem('splashShown')) {
            document.write('<style>#loading-screen { display: none !important; }</style>');
        }
    </script>
</head>
<body>
    <div id="loading-screen">
        <div class="loading-content">
            <img src="/Mueenk-logo-white.png" alt="Logo" class="loading-logo">
            
        </div>
    </div>

    <header class="hero-section" style="padding: 0;">
        <nav class="navbar">
            <div style="display: flex; align-items: center; gap: 12px;">
                <a href="/" class="nav-brand">
                    <img src="/Mueenk-logo-white.png" alt="Logo">
                    
                </a>
            </div>

            <div style="display: flex; gap: 10px; align-items: center;">
                <% if (locals.isPharmacistPage === true) { %>
                    <a href="/requests-list" class="btn-outline" style="font-size: 13px; border-radius: 12px; padding: 8px 15px;">الطلبات النشطة</a>
                    <a href="/completed-requests" class="btn-outline" style="font-size: 13px; border-radius: 12px; padding: 8px 15px;">الطلبات المكتملة</a>
                    <a href="/patient-lookup" class="btn-outline" style="font-size: 13px; border-radius: 12px; padding: 8px 15px;">بحث مريض</a>
                    <a href="/" class="btn-pharmacist-link" style="background: #e11d48; border: none; padding: 8px 15px;">خروج </a>

                <% } else { %>
                    
                    <% if (locals.isHomePage === true) { %>
                        <a href="/pharmacist-login" class="btn-pharmacist-link">بوابة الصيدلي </a>
                    <% } %>

                    <a href="/" class="btn-outline" style="font-size: 14px; border-radius: 15px; padding: 8px 20px;">الرئيسية</a>
                <% } %>
            </div>
        </nav>
    </header>

    <main class="app">