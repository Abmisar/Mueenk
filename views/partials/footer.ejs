</main>

    <footer class="footer">
        <p style="font-size: 18px; font-weight: 700;">منصة مُعينك - لخدمتكم دوماً</p>
        <div style="display: flex; justify-content: center; gap: 20px; margin-top: 20px;">
            <a href="mailto:info@mueenk.com" style="background: rgba(255,255,255,0.1); padding: 15px 25px; border-radius: 20px; color: white; text-decoration: none; display: flex; align-items: center; gap: 10px; transition: 0.3s;">
                <img src="/gmail.png" alt="Email" style="width: 24px; ">
                info@mueenk.com
            </a>
        </div>
    </footer>

    <script>
        window.addEventListener('load', function() {
            const loader = document.getElementById('loading-screen');
            if (loader) {
                if (!sessionStorage.getItem('splashShown')) {
                    setTimeout(() => {
                        loader.style.opacity = '0';
                        setTimeout(() => loader.style.display = 'none', 800);
                        sessionStorage.setItem('splashShown', 'true');
                    }, 2000);
                } else {
                    loader.style.display = 'none';
                }
            }
        });
    </script>
</body>
</html>
